spring:
    datasource:
        url: jdbc:mysql://localhost:3306/ghostnetdb
        username: dbuser
        password: 12345
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
            '[format_sql]': true

logging:
  level:
    '[org.springframework.web]': DEBUG
    '[org.springframework.http.converter.json]': DEBUG
    '[org.hibernate.SQL]': DEBUG
    #'[org.hibernate.type]': TRACE
